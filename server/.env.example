# 今天不低头 · 后端环境变量示例
# 复制为 server/.env 并按需填值。.env 已被 .gitignore 排除,key 绝不进 git。
# 后端启动时会自动加载 server/.env(无需手动 export),直接 `node server/index.js` 即可。
# 不填这个文件也能跑:默认 stub 模式,本地演示无需任何 key。

# provider 选择:stub(默认,本地演示无需 key) | jd-gateway(接口确认后)
LLM_PROVIDER=stub

# 端口(默认 3000)
# PORT=3000
# 默认只监听本机,避免把带 LLM 能力的接口暴露到局域网。确需局域网演示时显式设 HOST=0.0.0.0。
# HOST=127.0.0.1

# MaixCAM 独立设备网关。默认关闭；开启后只暴露 /device/v1/*，不要复用 LLM Key。
# DEVICE_GATEWAY_ENABLED=1
# DEVICE_HOST=0.0.0.0
# DEVICE_PORT=3180
# DEVICE_TOKEN=请生成一段至少32位的随机字符串

# —— 以下仅在 LLM_PROVIDER=jd-gateway 时需要,接口确认后填 ——
# JD_LLM_GATEWAY_URL=
# JD_LLM_GATEWAY_KEY=
# JD_LLM_MODEL=
